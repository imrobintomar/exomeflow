[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "exomeflow"
version = "2.2.8"
description = "Production-quality Whole Exome Sequencing analysis pipeline"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Robin Kumar", email = "itsrobintomar@gmail.com" }
]
keywords = ["bioinformatics", "WES", "NGS", "genomics", "exome", "variant-calling"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: POSIX :: Linux",
]
requires-python = ">=3.9"
dependencies = [
    "typer>=0.12.0",
    "rich>=13.0.0",
    "pandas>=2.0.0",
]

[project.optional-dependencies]
viz = ["matplotlib>=3.7.0"]
dev = ["pytest>=8.0.0", "pytest-mock>=3.12.0", "ruff>=0.6.0"]

[project.scripts]
exomeflow = "exomeflow.cli:app"

[project.urls]
Homepage = "https://pypi.org/project/exomeflow/"

[tool.setuptools.packages.find]
where = ["."]
include = ["exomeflow*"]

[tool.setuptools.package-data]
exomeflow = ["py.typed"]
